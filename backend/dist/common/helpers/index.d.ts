import * as fh from './formatHelper';
declare const helpers: {
    formatDate: typeof fh.formatDate;
    truncate: typeof fh.truncate;
    formatNumber: typeof fh.formatNumber;
    renderBadge: typeof fh.renderBadge;
    imageUrl: typeof fh.imageUrl;
};
export default helpers;
