<div class="navbar">
  <div class="nav-left">
    <button id="sidebarToggle" class="hamburger" aria-label="Buka menu" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
    <strong class="brand">ADAWEB</strong>
  </div>
  <nav class="nav-menu" aria-label="Primary">
    <a href="/">Home</a>
    <a href="/about">About</a>
    <details class="nav-dropdown" id="categoriesDropdown">
      <summary>
        Categories
        <span id="dropdownIcon" class="dropdown-icon" aria-hidden="true">▼</span>
      </summary>
      <div class="dropdown-panel">
        <a href="/categories/technology">Technology</a>
        <a href="/categories/business">Business</a>
        <a href="/categories/lifestyle">Lifestyle</a>
        <a href="/categories/entertainment">Entertainment</a>
        <a href="/categories/sports">Sports</a>
      </div>
    </details>
    <a class="theme-toggle" href="/switch-theme">Switch Theme</a>
  </nav>
</div>

<style>
  /* Horizontal nav menu */
  /* increase specificity so these styles win over more-generic rules */
  .navbar .nav-menu { display: flex; align-items: center; gap: 1rem; }
  .navbar .nav-menu a { padding: .4rem .6rem; border-radius: 6px; transition: color .1s; }
  .navbar .nav-menu a:hover { background: none; color: #4ECCA3; }

  /* Dropdown (no-JS using details/summary) */
  .nav-dropdown { position: relative; }
  .navbar .nav-dropdown summary { list-style: none; cursor: pointer; padding: .4rem .6rem; border-radius: 6px; display: flex; align-items: center; gap: 0.18em; transition: color .1s; }
  .nav-dropdown summary::-webkit-details-marker { display: none; }
  .navbar .nav-dropdown[open] > summary,
  .navbar .nav-dropdown summary:hover {
    background: none;
    color: #4ECCA3;
  }
  .navbar .nav-dropdown summary:hover .dropdown-icon,
  .navbar .nav-dropdown[open] > summary .dropdown-icon {
    color: #4ECCA3;
  }
  .dropdown-icon { color: #fff; font-size: 1em; margin-left: 0.18em; user-select: none; transition: color .1s; }
  .dropdown-panel { position: absolute; right: 0; top: calc(100% + 8px); background: #2a2f38; color: #f1f1f1; border: 1px solid #4a4f57; border-radius: 8px; padding: .5rem; min-width: 220px; box-shadow: 0 6px 18px rgba(0,0,0,.5); display: flex; flex-direction: column; }
  .dropdown-panel a { color: #f1f1f1; padding: .5rem .6rem; border-radius: 6px; }
  .dropdown-panel a:hover { background: #5A3B4E; color: #4ECCA3; }

  @media (max-width: 768px) {
    .nav-menu { gap: .6rem; flex-wrap: wrap; justify-content: flex-start; }
    .dropdown-panel { position: static; box-shadow: none; border: 0; padding: .25rem 0; background: transparent; }
    .dropdown-panel a { padding-left: 0; }
    .dropdown-icon { font-size: 1.1em; }
  }
</style>
<script>
  // Dropdown icon toggle (no animation, instant)
  document.addEventListener('DOMContentLoaded', function() {
    var dropdown = document.getElementById('categoriesDropdown');
    var icon = document.getElementById('dropdownIcon');
    if (dropdown && icon) {
      dropdown.addEventListener('toggle', function() {
        icon.textContent = dropdown.open ? '▲' : '▼';
      });
    }
  });
</script>
</style>
